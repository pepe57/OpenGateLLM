# Application configuration
CONFIG_FILE=api/tests/integ/config.test.yml

# Dependencies
## Replace POSTGRES_HOST by localhost to run integration tests locally
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

## Replace REDIS_HOST by localhost to run integration tests locally
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_USER=redis
REDIS_PASSWORD=changeme

## Replace ELASTIC_HOST by localhost to run integration tests locally
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_USER=elastic
ELASTICSEARCH_PASSWORD=changeme

RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Secrets (to complete)
#ALBERT_API_KEY=
#BRAVE_API_KEY=
